{
	"name": "tests",
	"type": "module",
	"version": "1.0.0",
	"devDependencies": {
		"@playwright/test": "^1.34.3"
	},
	"scripts": {
		"start": "cd .. && pnpm run start",
		"test": "npx playwright test",
		"codegen": "npx playwright codegen"
	}
}
